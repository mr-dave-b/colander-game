@model PlayerRenderModel;

@if (Model.NextUpPlayer == Model.Player.UserId)
{
    <b>
        @Model.Player.UserName
        @if (Model.Player.UserId == Model.CurrentUserId)
        {
            <text> (YOU)</text>
        }
    </b>
}
else
{
    @Model.Player.UserName
    @if (Model.Player.UserId == Model.CurrentUserId)
    {
        <text> (YOU)</text>
    }
}
